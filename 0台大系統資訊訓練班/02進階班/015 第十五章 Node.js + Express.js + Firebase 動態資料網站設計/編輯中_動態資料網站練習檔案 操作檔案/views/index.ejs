<!DOCTYPE html>
<html lang="en">

<head>
  <!-- 把HTML內容隔離出來，重複使用共同的HTML內容
  在shared/ 資料夾裡面 -->
  <% include shared/head %>
</head>

<body>
  <% include shared/navbar %>

    <header id="indexHeader" class="jumbotron header">
      <div class="container">
        <h1>Home Page</h1>
        <p>Lorem ipsum dolor sit amet.</p>
      </div>
    </header>
    <section class="py-3">
      <div class="container">
        <!-- 15.9從雲端資料庫取得集合資料並透過EJS模板渲染 -->
        <h1>商品列表</h1>
        <div class="row">
          
          <!-- forEach迴圈開頭 ，不斷產生相同的div-->
          <% productList.forEach(product=> { %>
            <div class="col-md-4">
              <div class="card mb-4">
                <img src="<%= product.image %>" class="card-img-top">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= product.name %>
                  </h5>
                  <p>
                    <%= product.category %>
                  </p>
                  <p>$ <%= product.price %> NTD</p>
                  <!-- 每個商品的按鈕網址是網址的ID -->
                  <a href="/product/show/<%= product.id %>" class="btn btn-primary">Detail</a>
                  <a href="/product/edit/<%= product.id %>" class="btn btn-warning">Edit</a>
                </div>
              </div>
            </div>
            <% }) %>
              <!-- forEach迴圈結尾 -->
        </div>
      </div>
    </section>
    <% include shared/scripts %>
</body>

</html>